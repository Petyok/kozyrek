import QtQuick
import Quickshell

ShellRoot {
    Bar {
        left:   [ Pill { Text { text: "🐐rek"; color: Theme.text; font.pixelSize: Theme.fontPx } } ]
        center: []
        right:  []
    }
}
